type User {
  id: ID
  name: String!
  email: String
  username: String!
  phone: String
  password: String!
  profile: Profile!
}

type Profile {
  bio: String
  birthday: String
  published: boolean
}

type Post {
  id: ID
  content: String
  title: String
  userid: User
}
